# Archie coordinator snapshot request

This branch differs from `agent/archie-coordinator-exact-snapshot-20260715` only by this marker. Its pull request exists solely to execute the read-only exact-tree archive workflow against the preserved coordinator tree rooted at `ba9a777504b96a49df29d2dac45988a3acbfb801`.

The second capture includes full Git ancestry so repository and frankenstate verification can execute against real commit history.

No runtime, product, deployment, secret, production-data, or coordinator-branch mutation is authorized by this marker.
