# Instrument Genesis Evidence Provenance

## Full receipt

`receipts/archie-instrument-genesis-full.json` retains the three-seed local CPU research run used to evaluate the declared architecture gate.

The numerical run was produced from the v3 implementation-equivalent local source during development. The receipt records the intended GitHub source blob, but the full numerical run was not repeated from a byte-for-byte checkout of that blob after publication formatting. It must therefore not be described as a byte-identical reproduction.

The dedicated GitHub Actions contract executes the committed GitHub source directly through:

- Python compilation;
- structural unit tests;
- an end-to-end CPU smoke run;
- verification that the reduced smoke budget cannot unlock teacher entry;
- structural validation of the retained full receipt.

A future independent full reproduction should check out the exact PR head, execute the full profile, and compare the resulting metrics and evolved program genealogies with the retained receipt. Until then, the correct evidence statement is:

> The implementation-equivalent v3 architecture passed the declared full three-seed local court, while the exact committed GitHub source passed its structural and end-to-end smoke contract.

## Claim boundary

The result establishes a bounded instrument-genesis mechanism in two synthetic partially observed dynamical world families. It does not establish unrestricted observable invention, semantic concepts, language competence, constitutional ontogenesis, open-ended development, or general intelligence.

The `full_teacher_entry` field authorizes only a bounded successor experiment in which proprietary-model outputs enter as provenance-tagged proposals. It is not admission to overwrite, reinterpret, or train on the teacher-free receipt.
