# Sideways repository revival

Issue #223 is the parent generation. This document is a human map only. The single root `.frankenstate` remains the canonical coordination ledger.

## Active lanes

| Lane | Issue | Purpose | Initial branch claim |
|---|---:|---|---|
| product | #224 | one coherent everyday Sideways journey | `revival/product-journey` |
| archive | #225 | private import, memory, backup, restore, and recovery | `revival/archive-survival` |
| social | #226 | public identity, publication, conversation, communities, and moderation | `revival/social-product` |
| ranking | #227 | inspectable controls and outcome evaluation | `revival/ranking-evaluation` |
| weave | #228 | complete running, dispatch, drain, terminal, and new-generation lifecycle | existing exact PR heads first |
| Maker | #229 | one real configured open-model engineering run | PR #222 first |
| operations | #230 | deployment, backup, restore, rollback, and external evidence | `revival/operations-proof` |
| quality | #231 | accessibility, browsers, performance, and degraded states | `revival/human-quality` |
| assembly | #232 | exact-head integration and final decision | `revival/repository-wide-generation` |

## Existing candidate vehicles

- PR #216: terminal drain release proof.
- PR #217: weave privacy and complete lasso history.
- PR #222: provider-neutral Maker execution path.

These are candidate inputs, not automatically accepted commits. Each moved head requires fresh exact-head proof.

## Parallel execution rule

1. A lane reads existing issues, branches, pull requests, and current main before editing.
2. A lane posts one branch claim and exact owned paths.
3. Distinct lanes may work simultaneously.
4. Overlap is recorded as a collision and resolved explicitly; it is not silently overwritten.
5. Child pull requests remain draft until their exact heads have complete receipts.
6. Assembly transports reviewed child heads; it does not recreate their work on the integration branch.
7. No model, comment, green workflow, or coordination receipt grants merge or deployment authority.

## Generation completion

The generation can reach a final decision only after:

- the main user journey is runnable on phone and desktop;
- private archive survival and public-social behavior are separately proven;
- ranking evaluation distinguishes demo proxies from measured outcomes;
- weave lifecycle and Maker execution each have real receipts;
- external operations and human-quality unknowns are named;
- the assembled exact head passes the complete repository matrix;
- issue #232 posts `MERGE RECOMMENDED`, `HOLD`, or `REJECT` against that exact head.
