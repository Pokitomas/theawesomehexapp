name: Archie causal repair synchronization marker

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This disposable marker is removed by the trusted PR integrator.'
