name: Archie causal repair synchronization marker

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Isolating current-main compatibility gates; this file is removed before admission.'
