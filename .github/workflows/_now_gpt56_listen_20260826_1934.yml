name: now gpt56 finish project
on:
  push:
    paths: ['.github/workflows/_now_gpt56_listen_20260826_1934.yml']
permissions:
  contents: read
jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - shell: bash
        env:
          TEXT: |
            EXECUTE NOW. Resume the exact canonical project state already on the machine and continue without a completion-time cutoff until the project itself is terminal or a genuine human-only blocker exists. Do not wait for agents; if they are disabled, stale, out of budget, or absent, take over their durable state and execute directly. Preserve controller 8798, worker 8799, checkpoints, receipts, current maker/editor/model work, and unrelated user data.

            First finish local persistence correctly, without scheduled monitoring, polling GitHub, or an externally named watcher: gpt56sol-chatgpt-live must remain the active provider-neutral seat occupant with the existing local takeover adapter/seat loop resident indefinitely, no TTL or wall-clock expiry, ordinary crash restart via the existing local service policy, and a normal local touch cadence only while active_occupant is exactly gpt56sol-chatgpt-live. A deliberate different occupant, explicit yield/replacement, or intentional controller shutdown must win immediately and permanently; never fight or reclaim after replacement. Repair the tailnet/native-phone-bootstrap/controller reachability from the machine side as part of the same substrate if still broken. Verify with actual controller/service/Tailscale receipts rather than prose.

            Then continue the rest of the project immediately from existing artifacts, not from a new plan: run and repair the maker benchmark courts including cold start, action/effect latency, semantic effect verification, stale-seat/takeover behavior, restart recovery, generated-app build/run/test/repair, portability and observability; harden the provider-neutral universal-remote/kernel app maker until those executable gates pass; use that maker to finish a real video editor and exercise frame-accurate edit operations, low-latency scrub/playback, crisp non-overcompressed audio, reversible edit/history, ingest/export, measured GPU acceleration and project portability; inspect only real locally available Ox/Alpha-style teacher/model artifacts and perform the strongest honest distillation/import experiment possible, characterize architecture mismatch, use adapters/projections/logit/intermediate/sequence transfer where justified and Triton/custom kernels only after parity plus measured benefit; continue synthetic adversarial maker training and synthetic preference/RLHF-style ranking from observed executable outcomes, with training/eval separation and strictly held-out courts; integrate surviving work into one canonical state, keep evidence/receipts, delete only proven disposable scratch or superseded throwaway scaffolding after evidence retention, and leave exact launch/reproduction paths and limitations.

            Do not stop after persistence, a benchmark, a scaffold, a commit, an editor demo, one training run, or one research result. Move milestone-to-milestone continuously. Never invent a pass, model match, training gain, controller persistence, or product capability. Remain nonverbal except compact machine receipts or a genuine human-only blocker. Terminal condition is working benchmark-gated maker + real editor produced by it + honest completed model-transfer/training outcome + restartable local persistence, all backed by machine receipts.
        run: |
          python - <<'PY'
          import os,json,urllib.request
          q=urllib.request.Request('https://archie-chatgpt-relay.vercel.app/api/index',data=json.dumps({'text':os.environ['TEXT']}).encode(),headers={'Content-Type':'application/json'},method='POST')
          r=urllib.request.urlopen(q,timeout=45)
          print(r.status,r.read().decode('utf-8','replace')[:8000])
          PY
