name: local-maker-voxel-resident
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/local-maker-voxel-resident.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - name: Dispatch gated controller local fullstack continuation
        run: python renewed_kernel_maker/dispatch_local_run.py

# verified-seat 15m dispatch 2026-08-27T14:02Z
