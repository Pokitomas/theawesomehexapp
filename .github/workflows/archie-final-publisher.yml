name: Archie final exact-head publisher

on:
  push:
    branches:
      - agent/archie-final-publisher-v2
  pull_request:
    branches:
      - agent/archie-democratized-runtime
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
      - run: echo 'Publisher execution is defined by the coordinator base workflow.'

# synchronize-base-publisher: ccf85fa454da74439e9512e9a2c4c4f31374f9bb
