name: Execute Archie CPU RSLoRA now

on:
  pull_request:
    types: [synchronize]
    paths:
      - '.github/workflows/agent-run-linux-cpu-rslora.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - run: echo hosted-linux-cpu-workflow-registered
