const CACHE='archie95-admitted-v4-20260721';
const CORE=['.','index.html','manifest.webmanifest','model-package.json','register-student-core-v4.mjs'];
self.addEventListener('install',event=>event.waitUntil((async()=>{const cache=await caches.open(CACHE);await cache.addAll(CORE);const manifest=await (await cache.match('model-package.json')).json();await Promise.all(manifest.model_parts.map(part=>cache.add(part)));await self.skipWaiting()})()));
self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys())if(key!==CACHE)await caches.delete(key);await self.clients.claim()})()));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;event.respondWith((async()=>{const cache=await caches.open(CACHE);const hit=await cache.match(event.request,{ignoreSearch:true});if(hit)return hit;try{const response=await fetch(event.request);if(response.ok&&new URL(event.request.url).origin===self.location.origin)cache.put(event.request,response.clone());return response}catch{return cache.match('index.html')}})())});
