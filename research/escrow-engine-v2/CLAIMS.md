# ESCROW Engine v2 — Claim Ledger

## Established in the finite explicit model

- A point artifact executes an action-indexed finite stochastic kernel.
- Installation rejects unless both artifact and reference kernels are explicit members of the declared finite credal family.
- Artifact execution obeys `z' <= dbar*z + width`.
- Exact-reference execution on the current approximate state obeys `z' <= delta(reference)*z`.
- A repeating artifact/reference block with a safe affine fixed point is safe indefinitely under the declared scalar gate.
- The included artifact expires after one standalone step at `tau=0.30`, while the synthesized one-artifact/one-reference cycle remains below `0.2272727273` indefinitely.
- The cycle costs `0.525` reference-step equivalents per step and therefore gives `1.9047619x` long-run speedup in the declared synthetic cost model.
- The 20-step finite planner chooses 10 artifact and 10 reference steps, costs 10.5 instead of 20.0, and remains cheaper after six setup equivalents.
- Runtime recomputes debt and cost transitions and arrests on plan or kernel tampering.
- Explicit two-kernel credal trajectories can grow as `2^t`; the scalar envelope survives without claiming that full set propagation is cheap.

## Corrected or withdrawn

- **Withdrawn:** the former runtime was an engine. It returned a selector token and did not execute a substitute.
- **Withdrawn:** family diameter automatically bounds an arbitrary artifact.
- **Withdrawn:** an exceptional row necessarily makes family width positive.
- **Withdrawn:** exact fallback resets accumulated debt to zero without exact state reacquisition.
- **Withdrawn:** affine-envelope composition certifies interfaces or jurisdictions.
- **Withdrawn:** full credal-set propagation is the cheap runtime.
- **Withdrawn:** the former NP-completeness theorem for the stated TV-realizable stochastic-kernel problem.
- **Demoted:** `kappa = width/(1-dbar)` is a stationary diagnostic, not a complete switched-runtime state.

## Not established

- Real neural or transformer deployment.
- Scalable artifact or quotient discovery.
- Statistical membership certification under continuous uncertainty.
- Literature-complete novelty.
- Global optimality outside the finite operator set and scalar debt abstraction.
- TV realizability of the withdrawn hardness construction.

## Positive gate

| Gate | Status |
|---|---|
| Nontrivial necessity/result | PASS |
| Executable new capability | PASS |
| Measured total-cost advantage | PASS, synthetic |
| Hostile baselines and anti-planted controls | PASS |
| Credible real neural deployment | FAIL |
| Literature collision does not dissolve contribution | OPEN / FAIL |

**Score: 4/6.** The code is worth retaining and testing. It is not yet a sealed ML breakthrough.
